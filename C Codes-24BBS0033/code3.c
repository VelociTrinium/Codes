//code to perform simple Multiplication
#include <stdio.h>
int main()
{
	int a,b,c;
	printf("Input the number you want to multiply \n");
	scanf("%d%d", &a,&b);
	c = a * b;
	printf("%d * %d = %d", a,b,c);
	return(0);
}
