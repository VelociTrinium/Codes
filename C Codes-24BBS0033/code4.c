//code to perform simple DIvide
#include <stdio.h>
int main()
{
	int a,b,c;
	printf("Input the number you want to divide \n");
	scanf("%d%d", &a,&b);
	c = a / b;
	printf("%d divided by %d = %d", a,b,c);
	return(0);
}
